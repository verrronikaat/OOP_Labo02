<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!-- Общие цвета и стили, чтобы интерфейс выглядел аккуратно (лабораторная работа). -->
    <Color x:Key="Brush.Primary">#2B579A</Color>
    <Color x:Key="Brush.Accent">#3FA7D6</Color>
    <Color x:Key="Brush.Surface">#F7F9FC</Color>
    <SolidColorBrush x:Key="PrimaryBrush" Color="{StaticResource Brush.Primary}"/>
    <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource Brush.Accent}"/>
    <SolidColorBrush x:Key="SurfaceBrush" Color="{StaticResource Brush.Surface}"/>

    <Style x:Key="LabTabControlStyle" TargetType="TabControl">
        <Setter Property="Background" Value="{StaticResource SurfaceBrush}"/>
        <Setter Property="Padding" Value="4"/>
    </Style>

    <Style TargetType="GroupBox">
        <Setter Property="Margin" Value="0,0,0,10"/>
        <Setter Property="Padding" Value="10"/>
        <Setter Property="BorderBrush" Value="#D0D7E2"/>
        <Setter Property="BorderThickness" Value="1"/>
    </Style>

    <Style TargetType="TextBlock">
        <Setter Property="TextWrapping" Value="Wrap"/>
        <Setter Property="Margin" Value="0,0,0,6"/>
    </Style>

    <Style x:Key="PrimaryButton" TargetType="Button">
        <Setter Property="Padding" Value="12,6"/>
        <Setter Property="Margin" Value="0,6,8,0"/>
        <Setter Property="Background" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Foreground" Value="White"/>
        <Setter Property="BorderBrush" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Cursor" Value="Hand"/>
    </Style>

    <Style x:Key="SectionTitle" TargetType="TextBlock">
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource PrimaryBrush}"/>
        <Setter Property="Margin" Value="0,0,0,8"/>
    </Style>
</ResourceDictionary>
