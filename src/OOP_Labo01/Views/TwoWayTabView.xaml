<UserControl x:Class="OOP_Labo01.Views.TwoWayTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="480" d:DesignWidth="780">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="8">
            <TextBlock Style="{StaticResource SectionTitle}" Text="Явный режим TwoWay: изменения идут в обе стороны."/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Прямая связь элементов: два TextBox с явным Mode=TwoWay -->
                <GroupBox Grid.Column="0" Header="Прямая привязка UI (TwoWay + ElementName)" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="Первое поле — источник имени для второго (дублирование в реальном времени)."/>
                        <TextBox x:Name="SourceBox" Text="Привет" Margin="0,8,0,4"/>
                        <TextBox Text="{Binding Text, ElementName=SourceBox, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0,0,0,4"/>
                        <Slider x:Name="DirectVolume" Minimum="0" Maximum="100" Value="55" Margin="0,12,0,4"/>
                        <TextBlock Margin="0,4,0,0">
                            <Run Text="Текущее значение слайдера: "/>
                            <Run Text="{Binding Value, ElementName=DirectVolume, Mode=OneWay}"/>
                        </TextBlock>
                    </StackPanel>
                </GroupBox>

                <!-- ViewModel: явно указан TwoWay -->
                <GroupBox Grid.Column="1" Header="Привязка через ViewModel (Mode=TwoWay)">
                    <StackPanel>
                        <TextBlock Text="TextBox ↔ UserName, Slider ↔ Volume (оба с явным TwoWay)."/>
                        <TextBox Text="{Binding UserName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 Margin="0,8,0,4"/>
                        <Label Content="{Binding UserName}" Margin="0,0,0,8"/>
                        <Slider Minimum="0" Maximum="100"
                                Value="{Binding Volume, Mode=TwoWay}"
                                TickFrequency="5" IsSnapToTickEnabled="True"/>
                        <TextBlock Margin="0,6,0,0">
                            <Run Text="Громкость (VM): "/>
                            <Run Text="{Binding Volume, Mode=OneWay}"/>
                        </TextBlock>
                    </StackPanel>
                </GroupBox>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
