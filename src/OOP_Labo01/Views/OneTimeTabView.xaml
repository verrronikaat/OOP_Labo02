<UserControl x:Class="OOP_Labo01.Views.OneTimeTabView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="480" d:DesignWidth="780">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="8">
            <TextBlock Style="{StaticResource SectionTitle}" Text="OneTime: значение передаётся один раз при создании привязки."/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <GroupBox Grid.Column="0" Header="Прямая привязка (OneTime)" Margin="0,0,8,0">
                    <StackPanel>
                        <TextBlock Text="TextBlock получает текст из TextBox только при первом отображении (поменяйте TextBox — подпись не следует)."/>
                        <TextBox x:Name="SeedBox" Text="Начальный текст" Margin="0,8,0,4"/>
                        <TextBlock FontSize="16" Margin="0,4,0,0"
                                   Text="{Binding Text, ElementName=SeedBox, Mode=OneTime}"/>
                    </StackPanel>
                </GroupBox>

                <GroupBox Grid.Column="1" Header="Привязка через ViewModel (OneTime)">
                    <StackPanel>
                        <TextBlock Text="Label и TextBox ниже используют OneTime к FrozenLabel. Кнопка меняет свойство в VM — визуально остаётся стартовое значение."/>
                        <Label FontSize="16" Margin="0,8,0,4"
                               Content="{Binding FrozenLabel, Mode=OneTime}"/>
                        <TextBox Text="{Binding FrozenLabel, Mode=OneTime}" Margin="0,0,0,8"/>
                        <Button Content="Изменить FrozenLabel в ViewModel" Style="{StaticResource PrimaryButton}"
                                Command="{Binding BumpFrozenCommand}"/>
                        <TextBlock Margin="0,8,0,0" Opacity="0.85" FontSize="12"
                                   Text="{Binding EditableVmField, Mode=OneWay}"/>
                    </StackPanel>
                </GroupBox>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</UserControl>
